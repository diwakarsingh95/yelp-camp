<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <!-- Favicon -->
    <link
      rel="apple-touch-icon"
      sizes="180x180"
      href="/image/apple-touch-icon.png"
    />
    <link
      rel="icon"
      type="image/png"
      sizes="32x32"
      href="/image/favicon-32x32.png"
    />
    <link
      rel="icon"
      type="image/png"
      sizes="16x16"
      href="/image/favicon-16x16.png"
    />
    <link rel="manifest" href="/manifest.json" />

    <title>YelpCamp</title>
    <meta
      name="description"
      content="YelpCamp - Online place for Campgrounds."
    />

    <!-- CSS Preload Links -->
    <link
      rel="preload"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      as="style"
    />
    <link rel="preload" href="/css/app.css" as="style" />
    <% if (!authPage) { %>
    <link
      rel="preload"
      href="https://api.mapbox.com/mapbox-gl-js/v2.5.1/mapbox-gl.css"
      as="style"
    />
    <% } %>

    <!-- Script Preload Links -->
    <link
      rel="preload"
      href="https://cdn.jsdelivr.net/npm/@popperjs/core@2.10.2/dist/umd/popper.min.js"
      as="script"
    />
    <link
      rel="preload"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.min.js"
      as="script"
    />
    <link rel="preload" href="/js/validateForms.js" as="script" />
    <% if (!authPage) { %>
    <link
      rel="preload"
      href="https://api.mapbox.com/mapbox-gl-js/v2.5.1/mapbox-gl.js"
      as="script"
    />
    <% } %>

    <!-- CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />

    <link rel="stylesheet" href="/css/app.css" />
    <% if (!authPage) { %>
    <link
      href="https://api.mapbox.com/mapbox-gl-js/v2.5.1/mapbox-gl.css"
      rel="stylesheet"
    />
    <% } %>

    <!-- Mapbox Script -->
    <% if (!authPage) { %>
    <script defer src="https://api.mapbox.com/mapbox-gl-js/v2.5.1/mapbox-gl.js"></script>
    <% } %>
  </head>

  <body class="d-flex flex-column min-vh-100">
    <% include ../partials/navbar %>
    <main class="container my-4">
      <% include ../partials/flash %> <%- body -%>
    </main>
    <% include ../partials/footer %>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.10.2/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.min.js"></script>
    <script src="/js/validateForms.js"></script>
  </body>
</html>
